/build/
*.zip

# Local-only design prototypes and scratch uploads
/docs/design/

# Device-specific secrets for our own test hardware. Never publish: anything here
# is expected to hold credentials, addresses or keys for a specific board.
/local/

# macOS Finder metadata (never publish)
.DS_Store
._*
.AppleDouble
.LSOverride

web/node_modules/

# Python bytecode
__pycache__/
*.py[cod]
